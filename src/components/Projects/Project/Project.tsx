import { Box, Flex, Heading, Text } from "@chakra-ui/react";

const Project = ({ children }: { children: string }) => {
  return <Flex flexFlow={"column"} w={"100%"}></Flex>;
};

export default Project;
